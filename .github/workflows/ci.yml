name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main, test-ci-fix]

permissions:
  contents: read

jobs:
  smoke:
    name: Smoke Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "CI works!"
